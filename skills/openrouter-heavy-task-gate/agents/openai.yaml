interface:
  display_name: "OpenRouter Heavy Task Gate"
  short_description: "Route heavy coding work with minimal prompts"
  default_prompt: "Use $openrouter-heavy-task-gate to decide whether to handle this task directly or escalate it to GLM 5.2 through OpenRouter."
